name: Dev Storage Service

on:
  push:
    tags: [ dev/storage-service ]

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: satackey/action-docker-layer-caching@v0.0.11

    - name: Set up Go
      uses: actions/setup-go@v2
      with:
        go-version: 1.15

    - name: Test
      run: make test service=storage-service

    - name: Login to Docker Hub
      uses: docker/login-action@v1
      with:
        username: ${{ secrets.DOCKER_HUB_USERNAME }}
        password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}

    - name: Build and push
      run: |
        make prepare service=storage-service
        make docker service=storage-service
        docker tag storage-service docker.io/agungdp22/storage-service:latest
        docker push docker.io/agungdp22/storage-service:latest
